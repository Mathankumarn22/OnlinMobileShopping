<!DOCTYPE html>

<html>
    <head>
        <meta name="viewport" content="width=device-width" />
        <link rel="stylesheet" type="text/css" href="~/Styles/Master.css" />
        <title>@ViewBag.Title</title>
    </head>
    <body style="background-image:url(/Images/background.jpg)">
        
        <header class="header" style="padding : 10px;">
            <div>
                @Html.ActionLink("LogIn", "Login", "Account")
                @*<nav style="float:right;">
                    <h2 class="link">
                        @Html.ActionLink("Register", "SignUp", "Account")

                        @Html.ActionLink("LogIn", "Login", "Account")
                       
                        
                    </h2>
                </nav>*@
                @if (HttpContext.Current.User.Identity.IsAuthenticated)
                {
                     if (HttpContext.Current.User.IsInRole("Admin"))
                     {
                      @Html.ActionLink("MobileDetails", "MobileDetails", "Mobile")

                     }
                    if (HttpContext.Current.User.IsInRole("User"))
                    {
                        @Html.ActionLink("Index", "Account")
                    }
                    @Html.ActionLink("Logout", "LogOff", "Account")
                }
                @*else
                {
                    @Html.ActionLink("LogIn", "LogIn", "Account")
                }*@


                <h1 class="headerText">Online Mobile Shopping</h1>



            </div>

        </header>
        <div>
            @RenderBody()
        </div>
    </body>



    @*<meta name="viewport" content="width=device-width" />
    <title>@ViewBag.Title</title>
    <link rel="stylesheet" href="~/Styles/StyleSheet.css"
    <center>
        <div style="width: 100%;height:50px;background-color:aliceblue">
            <h1> <span style="font-size:40px,medium">  @Html.ActionLink("Welcome To Online Mobile Shop", "Index", "Home")</span></h1>
        </div>
    </center>
</head>
<body style="height: 59px">
    <div class="Home">
        
        @Html.ActionLink("Register", "SignUp", "Account")

        @Html.ActionLink("LogIn", "Login", "Account")

        @Html.ActionLink("MobileDetails", "MobileDetails", "Mobile")

        @Html.ActionLink("DisplayUserDetails", "DisplayUserDetails", "Account")
        @Html.ActionLink("Add Brand", "AddBrand", "Mobile")
    </div>
    <div>
        @RenderBody()
    </div>

</body>*@
</html>
